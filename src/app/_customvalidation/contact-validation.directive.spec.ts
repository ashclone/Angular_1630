import { ContactValidationDirective } from './contact-validation.directive';

describe('ContactValidationDirective', () => {
  it('should create an instance', () => {
    const directive = new ContactValidationDirective();
    expect(directive).toBeTruthy();
  });
});
